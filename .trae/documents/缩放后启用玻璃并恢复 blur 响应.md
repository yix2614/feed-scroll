## 目标
- 缩放动画完成后再启用玻璃效果
- blur 在启用后可实时响应 App.tsx 的参数变化

## 实施步骤
1. 在视频卡片容器上监听动画结束事件（animationend），用其替代固定 1.6s 计时器。
2. 将玻璃启用条件改为“动画完成”标志位，移除定时器依赖。
3. 保持玻璃启用后不再阻断 blur 参数更新。

## 验证
- 预览确认缩放完成后玻璃生效
- 修改 App.tsx 的 blur 数值后立即看到变化